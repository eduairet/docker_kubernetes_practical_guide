# Laravel & PHP Dockerized Project

## Structure

- `app/`: Laravel application files
  - Composer (package manager) files
  - Laravel artisan
  - NPM
- `PHP` interpreter
- `Nginx` web server
- `MySQL` database
